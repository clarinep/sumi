[package]
name = "koma"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = { version = "1.12.1", default-features = false }
crossbeam-queue = "0.3.13"

[profile.release]
lto = "fat"
opt-level = 3
codegen-units = 1
strip = "symbols"

[profile.dev]
debug = false
